meta {
  name: GET /service by relatedParty
  type: http
  seq: 8
}

get {
  url: {{tmfc008_base_url}}/service?filter=$.relatedParty[?(@.partyOrPartyRole.name=='Peter Meyer')]
  body: none
  auth: inherit
}

params:query {
  filter: $.relatedParty[?(@.partyOrPartyRole.name=='Peter Meyer')]
}

settings {
  encodeUrl: true
  timeout: 0
}
